{% extends 'base.html.twig' %}

{% block title %}Gardening{% endblock %}

{% block body %}
    <h1>Gardening</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gardening.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gardening_index') }}">back to list</a>

    <a href="{{ path('app_gardening_edit', {'id': gardening.id}) }}">edit</a>

    {{ include('gardening/_delete_form.html.twig') }}
{% endblock %}
