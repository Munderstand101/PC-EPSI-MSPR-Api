{% extends 'base.html.twig' %}

{% block title %}Botanist{% endblock %}

{% block body %}
    <h1>Botanist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ botanist.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ botanist.name }}</td>
            </tr>
            <tr>
                <th>Specialization</th>
                <td>{{ botanist.specialization }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ botanist.address }}</td>
            </tr>
            <tr>
                <th>Zipcode</th>
                <td>{{ botanist.zipcode }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ botanist.city }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ botanist.longitude }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ botanist.latitude }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_botanist_index') }}">back to list</a>

    <a href="{{ path('app_botanist_edit', {'id': botanist.id}) }}">edit</a>

    {{ include('botanist/_delete_form.html.twig') }}
{% endblock %}
